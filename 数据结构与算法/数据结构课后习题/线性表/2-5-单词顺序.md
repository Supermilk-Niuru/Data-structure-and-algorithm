### 题目
编写一个算法，判断两个存储在顺序线性表的单词在字典中谁在前，谁在后
##### 注意事项
1. 三目运算符的使用 ? 1 : 2 前面为真，返回$1$，前面为假，返回$2$
2. `string`是字符串 `char`是字符
3. 谁更小，打印谁，不要弄错逻辑
4. 相等的情况，如果全部相等，那么再比较长度。
5. 注意，循环打印的时候注意下标`i` `j` 
6. 关于`if-else` 的逻辑，如果已经发现了`A`在`B`的前面 直接跳出当前程序，如果只写`break`，则会继续执行后面的`if-else`
